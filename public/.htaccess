RewriteEngine On

# Si fichier ou dossier existe, on sert directement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, on redirige tout vers index.php
RewriteRule ^ index.php [QSA,L]
